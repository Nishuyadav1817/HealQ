/**
 * UpcharGanga's patient-facing Card. Re-exports the shared components/ui/Card
 * so every panel shares one card treatment (radius, padding, border,
 * shadow, selected/hover states) instead of a patient-only fork.
 */
export { default } from '../../../../components/ui/Card';
