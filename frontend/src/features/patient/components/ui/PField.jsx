/**
 * UpcharGanga's patient-facing Field. Re-exports the shared components/ui/Field
 * so labels, inputs, focus rings, and error states look identical across
 * every panel rather than diverging per surface.
 */
export { default } from '../../../../components/ui/Field';
